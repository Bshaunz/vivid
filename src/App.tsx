import MorningLog from "@/screens/MorningLog";

/**
 * Phase 2 in progress: Morning Log is the only screen. The router + bottom
 * nav shell replaces this direct render when the remaining screens land.
 */
export default function App() {
  return <MorningLog />;
}
